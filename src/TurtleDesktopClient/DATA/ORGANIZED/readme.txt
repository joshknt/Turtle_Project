This folder contains folders for each terminal. The folders are named 6 digit numbers, which are the first 6 digits of the board ID.
The first three digits represent the country code. The next three represent the terminal number. There should be a folder for every
terminal.