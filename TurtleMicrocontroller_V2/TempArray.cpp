#include "TempArray.h"

TempArray::TempArray()
{
  bottom  = -1;
  middle  = -1;
  top     = -1;
}

TempArray::TempArray(int bottom, int middle, int top)
{
  bottom  = bottom;
  middle  = middle;
  top     = top;
}

char* TempArray::ToString(TempArray a)
{
  char* buffer;

  return buffer;
}

